name: hawc_env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - sasktran2
  - xarray
  - scipy
  - numpy
  - matplotlib
  - pandas
  - cartopy
  - numba
  - dask
  - pip
  - pip:
    - hawcsimulator  # pure Python, safe to install from PyPI
